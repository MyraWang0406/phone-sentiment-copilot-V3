# 🚀 完整部署指南：GitHub → Render → Netlify

## 📋 部署流程概览

1. ✅ **GitHub**：将代码推送到 GitHub 仓库
2. ✅ **Render**：部署后端 API 到 Render
3. ✅ **Netlify**：部署前端到 Netlify

---

## 第一步：准备项目并推送到 GitHub

### 1.1 检查项目结构

确保以下文件都在项目根目录（`ZDM+Reddit/`）：
- ✅ `main.py` - 入口文件（已创建）
- ✅ `requirements.txt` - Python 依赖
- ✅ `render.yaml` - Render 配置文件
- ✅ `Global_Phone_Sentiment/` - 数据目录
- ✅ `netlify-deploy/` - 前端目录
- ✅ 根目录的 Reddit CSV 文件

### 1.2 初始化 Git 仓库（如果还没有）

在项目根目录打开 PowerShell 或 CMD：

```powershell
# 检查是否已有 Git 仓库
git status

# 如果没有，初始化仓库
git init

# 创建 .gitignore（如果还没有）
# 注意：确保不要提交 tmp_browser_data 等临时文件
```

### 1.3 创建 .gitignore（推荐）

在项目根目录创建 `.gitignore` 文件：

```
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/

# 临时浏览器数据
**/tmp_browser_data*/
**/tmp_browser_data_bilibili*/

# IDE
.vscode/
.idea/
*.swp
*.swo

# 日志和进度文件
*.log
*_progress.json
```

### 1.4 提交代码到 Git

```powershell
# 添加所有文件（.gitignore 会自动排除不需要的文件）
git add .

# 提交代码
git commit -m "Initial commit: Phone Sentiment API for Render deployment"

# 查看要提交的文件
git status
```

### 1.5 在 GitHub 上创建仓库

1. 访问 https://github.com
2. 点击右上角 "+" → "New repository"
3. 填写仓库信息：
   - Repository name: 例如 `phone-sentiment-api`
   - 选择 Public 或 Private
   - **不要**勾选 "Add a README file"（因为本地已有代码）
4. 点击 "Create repository"

### 1.6 关联并推送代码

```powershell
# 关联远程仓库（替换为你的实际仓库地址）
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 查看远程仓库
git remote -v

# 推送到 GitHub
git branch -M main
git push -u origin main
```

**如果遇到认证问题**：
- 使用 GitHub Personal Access Token（推荐）
- 或者使用 SSH 方式：`git@github.com:你的用户名/你的仓库名.git`

---

## 第二步：部署后端到 Render

### 2.1 登录 Render

1. 访问 https://render.com
2. 点击 "Get Started for Free"
3. 使用 GitHub 账号登录（推荐）

### 2.2 创建 Web Service

1. 在 Render Dashboard，点击 **"New +"** 按钮
2. 选择 **"Web Service"**

3. **连接 GitHub 仓库**：
   - 如果第一次使用，需要授权 Render 访问 GitHub
   - 选择你刚才推送的仓库

4. **配置服务**：

   **情况 A：如果 Render 自动识别了 `render.yaml`**
   - Render 会自动检测到配置文件
   - 直接点击 **"Apply"** 或 **"Create Web Service"**
   - 所有配置会自动从 `render.yaml` 读取

   **情况 B：如果没有自动识别，手动配置**：
   - **Name**：`phone-sentiment-api`（或你喜欢的名称）
   - **Region**：选择 `Frankfurt` 或 `Singapore`（推荐选择离你较近的地区）
   - **Branch**：选择 `main`
   - **Root Directory**：留空（使用项目根目录）
   - **Runtime**：选择 `Python 3`
   - **Build Command**：`pip install -r requirements.txt`
   - **Start Command**：`uvicorn main:app --host 0.0.0.0 --port $PORT`
   - **Plan**：选择 `Free`（免费计划）
   - **Auto-Deploy**：选择 `Yes`（代码推送后自动重新部署）

5. 点击 **"Create Web Service"**

### 2.3 等待部署完成

1. Render 会开始构建和部署，过程可能需要 3-5 分钟
2. 在 "Events" 或 "Logs" 标签页查看部署进度
3. 看到 "Your service is live" 提示表示部署成功

### 2.4 获取后端 URL

部署成功后：
- 在 Render Dashboard 可以看到服务状态为 **"Live"**
- 服务 URL 会显示在页面顶部，格式类似：
  ```
  https://phone-sentiment-api.onrender.com
  ```
- **请复制这个 URL，后面会用到！**

### 2.5 测试后端 API

访问以下地址测试：
- `https://你的服务名.onrender.com/health` - 健康检查
- `https://你的服务名.onrender.com/docs` - API 文档
- `https://你的服务名.onrender.com/stats` - 统计数据

---

## 第三步：部署前端到 Netlify

### 3.1 更新前端 API 地址

1. 打开 `netlify-deploy/index.html` 文件
2. 找到第 1042 行左右：
   ```javascript
   const API_BASE = "https://your-backend-api.com";
   ```
3. 将其改为你的 Render 后端 URL：
   ```javascript
   const API_BASE = "https://phone-sentiment-api.onrender.com";
   ```
   **注意**：不要加末尾的斜杠 `/`

4. 保存文件

### 3.2 部署到 Netlify

**方式一：拖拽上传（最简单）**

1. 将整个 `netlify-deploy` 文件夹压缩成 ZIP 文件
2. 访问 https://app.netlify.com 并登录
3. 点击 **"Add new site"** → **"Deploy manually"**
4. 将 ZIP 文件拖拽到上传区域
5. 等待部署完成（通常 1-2 分钟）
6. Netlify 会自动分配一个 URL，例如：
   ```
   https://random-name-12345.netlify.app
   ```

**方式二：通过 Git 仓库**

1. 在 GitHub 创建一个单独的仓库用于前端
2. 将 `netlify-deploy` 文件夹的内容推送到该仓库
3. 在 Netlify 选择 **"Import from Git"**
4. 选择前端仓库
5. 设置：
   - **Build command**：留空
   - **Publish directory**：`.`（当前目录）
6. 点击 **"Deploy site"**

### 3.3 访问前端

部署成功后，访问 Netlify 提供的 URL 即可使用完整的前端应用！

---

## ✅ 部署完成检查清单

### 后端（Render）
- [ ] 服务状态显示为 "Live"
- [ ] `/health` 可以访问并返回 `{"status": "ok"}`
- [ ] `/docs` 可以打开 API 文档页面
- [ ] `/stats` 可以返回统计数据

### 前端（Netlify）
- [ ] 页面可以正常加载
- [ ] KPI 卡片显示数据
- [ ] 品牌舆情概览表格有数据
- [ ] 选择品牌后可以查看评论详情
- [ ] 浏览器控制台（F12）没有 CORS 错误

---

## 🔧 本地测试（可选但推荐）

在部署前，建议先在本地测试：

### Windows PowerShell 命令

```powershell
# 1. 创建虚拟环境
python -m venv .venv

# 2. 激活虚拟环境
.venv\Scripts\Activate.ps1

# 如果报错，尝试：
# .venv\Scripts\activate.bat

# 3. 安装依赖
pip install -r requirements.txt

# 4. 启动服务
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

测试访问：
- http://localhost:8000/ - API 根路径
- http://localhost:8000/docs - API 文档
- http://localhost:8000/health - 健康检查

---

## ⚠️ 重要提示

### 1. 数据文件
- ✅ 确保所有 CSV 数据文件都提交到了 GitHub
- ✅ Render 会从 GitHub 拉取代码，所以数据文件也需要在仓库里

### 2. Render 免费计划限制
- ⏰ 如果服务 15 分钟没有活动，会自动休眠
- ⏱️ 首次访问休眠后的服务可能需要等待 30-60 秒唤醒
- 💡 可以通过定期访问健康检查接口来保持服务活跃

### 3. 端口配置
- ✅ Render 使用环境变量 `$PORT` 来指定端口
- ✅ 启动命令已配置为使用 `$PORT`，无需修改

### 4. CORS 配置
- ✅ 后端已配置允许所有来源（`allow_origins=["*"]`）
- ✅ Netlify 前端可以正常访问 Render 后端 API

---

## 🐛 常见问题

### GitHub 推送失败

**问题**：`git push` 时提示认证失败

**解决**：
1. 使用 GitHub Personal Access Token：
   - GitHub Settings → Developer settings → Personal access tokens
   - 创建新的 token，勾选 `repo` 权限
   - 推送时使用 token 作为密码

2. 或使用 SSH：
   ```powershell
   git remote set-url origin git@github.com:你的用户名/你的仓库名.git
   ```

### Render 部署失败

**问题**：Build 失败或启动失败

**解决**：
1. 查看 Render 的 Build Logs，找出错误信息
2. 检查 `requirements.txt` 中的依赖版本是否兼容
3. 确认 `render.yaml` 格式正确
4. 确认所有必要的文件都已提交到 GitHub

**常见错误**：
- `ModuleNotFoundError`：检查 `requirements.txt` 是否包含所有依赖
- 路径错误：确认 CSV 文件路径正确
- 端口错误：确认启动命令使用 `$PORT`

### 前端无法连接后端

**问题**：前端页面加载，但数据显示不出来

**解决**：
1. 打开浏览器开发者工具（F12），查看 Console 标签
2. 检查是否有 CORS 错误
3. 确认前端 `API_BASE` 地址正确（没有末尾斜杠）
4. 确认后端服务是 "Live" 状态
5. 测试后端 URL 是否可以直接访问

### 服务休眠问题

**问题**：访问时提示服务正在启动

**解决**：
- 这是 Render 免费计划的正常行为
- 等待 30-60 秒让服务唤醒
- 或升级到付费计划避免休眠

---

## 📝 文件清单

### 已创建/修改的文件：

1. **`main.py`** ✅（新建，项目根目录）
   - 入口文件，导入 `Global_Phone_Sentiment/main.py` 中的 app

2. **`Global_Phone_Sentiment/main.py`** ✅（已修改路径）
   - FastAPI 应用主文件
   - 路径已调整为支持从项目根目录运行

3. **`render.yaml`** ✅（已存在）
   - Render 部署配置文件

4. **`requirements.txt`** ✅（已存在）
   - Python 依赖列表

5. **`完整部署指南.md`** ✅（新建，本文档）

---

## 🎉 部署成功后的访问地址

### 后端 API（Render）
- **API 根路径**：`https://你的服务名.onrender.com/`
- **API 文档**：`https://你的服务名.onrender.com/docs`
- **健康检查**：`https://你的服务名.onrender.com/health`

### 前端应用（Netlify）
- **前端 URL**：`https://你的服务名.netlify.app`

---

## 📞 需要帮助？

如果遇到问题：
1. 查看 Render 的 Build Logs 和 Runtime Logs
2. 查看 Netlify 的部署日志
3. 检查浏览器控制台（F12）的错误信息
4. 参考本文档的"常见问题"部分

**祝你部署顺利！** 🚀

