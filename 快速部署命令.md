# ⚡ 快速部署命令清单

## 🎯 三步走：GitHub → Render → Netlify

---

## 第一步：推送到 GitHub

在项目根目录（`ZDM+Reddit/`）打开 PowerShell：

```powershell
# 1. 初始化 Git（如果还没有）
git init

# 2. 添加所有文件
git add .

# 3. 提交代码
git commit -m "Initial commit: Phone Sentiment API"

# 4. 在 GitHub 上创建新仓库后，关联远程仓库
# （替换为你的实际仓库地址）
git remote add origin https://github.com/你的用户名/你的仓库名.git

# 5. 推送到 GitHub
git branch -M main
git push -u origin main
```

**提示**：如果推送时提示认证失败，使用 GitHub Personal Access Token 或 SSH。

---

## 第二步：部署到 Render

### 在 Render 网站操作：

1. 访问 https://render.com 并登录（使用 GitHub 账号）

2. 点击 **"New +"** → **"Web Service"**

3. 选择你的 GitHub 仓库

4. **如果 Render 自动识别了 `render.yaml`**：
   - 直接点击 **"Create Web Service"**
   - ✅ 完成！

5. **如果没有自动识别，手动设置**：
   - **Name**: `phone-sentiment-api`
   - **Region**: `Frankfurt` 或 `Singapore`
   - **Branch**: `main`
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`
   - **Plan**: `Free`
   - 点击 **"Create Web Service"**

6. 等待 3-5 分钟部署完成

7. **复制后端 URL**，例如：
   ```
   https://phone-sentiment-api.onrender.com
   ```

---

## 第三步：部署前端到 Netlify

### 3.1 更新前端 API 地址

编辑 `netlify-deploy/index.html` 第 1042 行：

```javascript
// 改为你的 Render 后端 URL
const API_BASE = "https://phone-sentiment-api.onrender.com";
```

### 3.2 部署到 Netlify

**方式一：拖拽上传（最简单）**

1. 将 `netlify-deploy` 文件夹压缩成 ZIP
2. 访问 https://app.netlify.com
3. 点击 **"Add new site"** → **"Deploy manually"**
4. 拖拽 ZIP 文件上传
5. 等待部署完成，获得前端 URL

**方式二：Git 部署**

1. 创建新的 GitHub 仓库用于前端
2. 将 `netlify-deploy` 内容推送上去
3. 在 Netlify 选择 **"Import from Git"**
4. 选择前端仓库
5. 部署完成

---

## ✅ 完成！

### 访问地址：

- **后端 API**: `https://你的服务名.onrender.com`
- **前端应用**: `https://你的服务名.netlify.app`

---

## 🔍 快速测试

### 测试后端：
```
https://你的服务名.onrender.com/health
https://你的服务名.onrender.com/docs
```

### 测试前端：
打开前端 URL，检查：
- ✅ 页面正常加载
- ✅ KPI 卡片有数据
- ✅ 品牌表格显示数据
- ✅ 选择品牌后可以查看评论

---

## ⚠️ 如果遇到问题

1. **GitHub 推送失败**：使用 Personal Access Token 或 SSH
2. **Render 部署失败**：查看 Build Logs，检查依赖和路径
3. **前端无法连接后端**：检查浏览器控制台（F12）的错误信息

详细说明请查看：`完整部署指南.md`

