# 🚀 快速部署清单

## ✅ 配置检查完成

所有配置已准备就绪：
- ✅ `requirements.txt` 已合并并更新
- ✅ `main.py` 入口文件检查通过
- ✅ `render.yaml` 配置正确
- ✅ `netlify.toml` 已更新（包含 API 代理配置）

---

## 📋 部署步骤

### 1️⃣ 推送代码到 GitHub

```bash
git add .
git commit -m "准备部署到 Render + Netlify"
git push origin main
```

---

### 2️⃣ 部署后端到 Render

#### 推荐：使用 render.yaml（自动配置）

1. 访问 https://render.com → 登录
2. 点击 **"New +"** → **"Blueprint"**
3. 连接 GitHub 仓库
4. Render 会自动检测 `render.yaml` 并创建服务
5. 点击 **"Apply"** → 等待部署完成（3-5 分钟）
6. **复制服务 URL**（例如：`https://phone-sentiment-api.onrender.com`）

#### 或者：手动创建 Web Service

在 Render Dashboard 中：
- **Root Directory**：留空（使用根目录）
- **Build Command**：`pip install -r requirements.txt`
- **Start Command**：`uvicorn main:app --host 0.0.0.0 --port $PORT`
- **Plan**：`Free`

---

### 3️⃣ 更新前端 API 地址

#### 编辑 `netlify-deploy/index.html`（约第 1053 行）：

```javascript
// 改为你的 Render 后端地址
const API_BASE = "https://你的Render服务名.onrender.com";
```

#### 提交更改：

```bash
git add netlify-deploy/index.html
git commit -m "更新前端 API_BASE 为 Render 地址"
git push origin main
```

---

### 4️⃣ 部署前端到 Netlify

#### 方式 A：拖拽上传（推荐，最简单）

1. 访问 https://app.netlify.com → 登录
2. 点击 **"Add new site"** → **"Deploy manually"**
3. 将 `netlify-deploy` 文件夹压缩成 ZIP
4. 拖拽 ZIP 文件上传
5. 等待部署完成（1-2 分钟）

#### 方式 B：Git 自动部署

1. 访问 https://app.netlify.com → 登录
2. 点击 **"Add new site"** → **"Import an existing project"**
3. 选择 GitHub → 授权 → 选择仓库
4. 配置：
   - **Base directory**：留空
   - **Build command**：留空
   - **Publish directory**：`netlify-deploy`
5. 点击 **"Deploy site"**

---

### 5️⃣ 验证部署

#### 后端验证：
- ✅ `https://你的Render服务名.onrender.com/health` → 返回 `{"status": "ok"}`
- ✅ `https://你的Render服务名.onrender.com/docs` → 打开 API 文档

#### 前端验证：
- ✅ 访问 Netlify URL → 页面正常加载
- ✅ KPI 卡片显示数据
- ✅ 品牌表格显示品牌列表
- ✅ 浏览器控制台（F12）无错误

---

## 🔧 手动触发重新部署

### Render：
Dashboard → 你的服务 → **"Manual Deploy"** → **"Deploy latest commit"**

### Netlify：
Dashboard → 你的站点 → **"Deploys"** → **"Trigger deploy"**

---

## ⚠️ 重要提醒

1. **Render 免费计划**：服务可能休眠，首次访问需等待 30-60 秒唤醒
2. **CORS**：后端已配置允许所有来源，前端可正常调用
3. **API 地址**：如果更改 Render URL，记得同步更新前端 `API_BASE`

---

**详细说明请查看：`部署配置检查报告.md`**

