# 部署检查清单

## ✅ 部署前检查

### 1. 文件完整性
- [x] `package.json` - 依赖配置
- [x] `vite.config.ts` - 构建配置
- [x] `tsconfig.json` - TypeScript 配置
- [x] `tailwind.config.js` - 样式配置
- [x] `netlify.toml` - Netlify 部署配置
- [x] `index.html` - 入口文件
- [x] `src/` - 所有源代码
- [x] `src/data/` - Mock 数据文件

### 2. 构建测试
```bash
npm run build
```
- [x] 构建成功
- [x] `dist/` 目录已生成
- [x] 无 TypeScript 错误
- [x] 无 ESLint 错误

### 3. Git 配置
- [ ] `.gitignore` 已配置（排除 node_modules, dist）
- [ ] 所有源代码已提交
- [ ] 已推送到 GitHub

### 4. Netlify 配置

#### 如果项目在根目录（推荐）
- Base directory: **留空**
- Build command: `npm run build`
- Publish directory: `dist`

#### 如果项目在 frontend 子目录
- Base directory: `frontend`
- Build command: `npm run build`
- Publish directory: `frontend/dist`

## 🚀 快速部署步骤

### 1. 提交到 GitHub
```bash
cd frontend  # 如果在 frontend 目录
git add .
git commit -m "Ready for Netlify deployment"
git push origin main
```

### 2. 在 Netlify 中部署
1. 访问 https://app.netlify.com/
2. 点击 "Add new site" → "Import an existing project"
3. 选择 GitHub → 选择仓库
4. 配置构建设置（见上方）
5. 点击 "Deploy site"

### 3. 等待部署完成
- 查看构建日志
- 确认部署成功
- 访问提供的 URL 验证

## 🔧 如果部署失败

### 检查构建日志
1. 在 Netlify 中查看 "Deploy log"
2. 查找错误信息
3. 常见错误：
   - `npm: command not found` → 检查 Node 版本
   - `Module not found` → 检查依赖是否完整
   - `Build failed` → 检查 TypeScript 错误

### 本地测试构建
```bash
# 清理并重新构建
rm -rf dist node_modules
npm install
npm run build
```

## 📝 当前配置摘要

- **构建工具**: Vite
- **Node 版本**: 18 (见 .nvmrc)
- **输出目录**: dist
- **路由模式**: SPA (单页应用)
- **重定向**: 所有路由重定向到 index.html

## ✅ 部署后验证

访问 Netlify 提供的 URL，检查：
- [ ] 页面正常加载
- [ ] 顶部导航显示正常
- [ ] Tab 切换功能正常
- [ ] KPI 卡片数据正常
- [ ] 品牌表格正常显示
- [ ] 评论筛选器正常
- [ ] AI 面板正常
- [ ] 响应式布局正常

