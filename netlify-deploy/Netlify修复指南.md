# 🔧 Netlify 404 错误修复指南

## 问题分析

Netlify 显示 404 的常见原因：

1. **Base directory 配置错误**（最常见）
2. **Publish directory 配置错误**
3. **文件结构不正确**
4. **netlify.toml 位置不对**

---

## ✅ 解决方案

### 方案 1：修复 GitHub 自动部署配置（推荐）

#### 步骤 1：进入 Netlify Dashboard

1. 访问 https://app.netlify.com
2. 登录并进入你的站点：`phone-sentiment-copilot-v3-1`

#### 步骤 2：修改部署设置

1. 点击 **"Site settings"**（站点设置）
2. 在左侧菜单找到 **"Build & deploy"**
3. 找到 **"Build settings"** 部分
4. 点击 **"Edit settings"**

#### 步骤 3：配置正确的路径

**重要配置**：

- **Base directory**: `netlify-deploy` ⚠️ 必须设置！
- **Build command**: 留空或 `echo "No build needed"`
- **Publish directory**: `.` 或留空（相对于 base directory）

**说明**：
- Base directory 告诉 Netlify 从哪个文件夹开始构建
- Publish directory 告诉 Netlify 发布哪个文件夹的内容
- 因为 `netlify.toml` 在 `netlify-deploy` 文件夹内，所以 Base directory 必须是 `netlify-deploy`
- Publish directory 设置为 `.` 表示发布 base directory 本身的内容

#### 步骤 4：保存并重新部署

1. 点击 **"Save"**
2. 回到 **"Deploys"** 标签
3. 点击 **"Trigger deploy"** → **"Deploy site"**
4. 等待部署完成

---

### 方案 2：手动上传 ZIP（如果自动部署一直失败）

#### 步骤 1：准备正确的 ZIP 文件

**⚠️ 重要**：必须压缩 `netlify-deploy` 文件夹**内部的文件**，而不是文件夹本身！

**方法 A：使用 PowerShell 脚本**
```powershell
cd netlify-deploy
.\快速部署.ps1
```

**方法 B：手动操作**
1. 进入 `netlify-deploy` 文件夹
2. 全选所有文件（`index.html`、`netlify.toml` 等）
3. 右键 → "发送到" → "压缩(zipped)文件夹"
4. 将生成的 ZIP 文件重命名为 `netlify-deploy.zip`

**验证 ZIP 文件结构**：
打开 ZIP 文件，应该直接看到：
- `index.html` ✅
- `netlify.toml` ✅
- 其他文件...

**不应该看到**：
- `netlify-deploy/index.html` ❌（这是错误的）

#### 步骤 2：上传到 Netlify

1. 访问 https://app.netlify.com
2. 进入你的站点
3. 点击 **"Deploys"** 标签
4. 点击 **"Trigger deploy"** → **"Deploy site"**
5. 选择 **"Deploy manually"**
6. 将 `netlify-deploy.zip` 拖拽到上传区域
7. 等待部署完成

---

### 方案 3：使用 Netlify CLI（高级）

```powershell
# 1. 安装 Netlify CLI
npm install -g netlify-cli

# 2. 登录
netlify login

# 3. 进入 netlify-deploy 文件夹
cd netlify-deploy

# 4. 部署
netlify deploy --prod
```

---

## 🔍 验证部署

部署成功后：

1. **访问站点**
   - 应该能看到页面，而不是 404

2. **检查文件**
   - 访问 `https://你的域名.netlify.app/index.html`
   - 应该能正常显示

3. **检查 API**
   - 打开浏览器开发者工具（F12）→ Network 标签
   - 点击页面上的 "刷新数据" 按钮
   - 检查 API 请求是否正常

---

## 🐛 如果还是 404

### 检查清单

- [ ] Base directory 设置为 `netlify-deploy`
- [ ] Publish directory 设置为 `.` 或留空
- [ ] `netlify.toml` 在 `netlify-deploy` 文件夹内
- [ ] `index.html` 在 `netlify-deploy` 文件夹内
- [ ] ZIP 文件结构正确（如果手动上传）

### 查看部署日志

1. 在 Netlify Dashboard → **"Deploys"** 标签
2. 点击最新的部署
3. 查看 **"Deploy log"**
4. 检查是否有错误信息

### 常见错误

**错误 1：`No files found in publish directory`**
- 原因：Publish directory 配置错误
- 解决：设置为 `.` 或留空

**错误 2：`netlify.toml not found`**
- 原因：Base directory 配置错误
- 解决：设置为 `netlify-deploy`

**错误 3：`404 on all routes`**
- 原因：文件结构不正确
- 解决：检查 ZIP 文件结构或 Base directory 配置

---

## 📞 需要帮助？

如果以上方法都不行，请提供：
1. Netlify 部署日志截图
2. 站点设置中的 Build settings 截图
3. 具体的错误信息

