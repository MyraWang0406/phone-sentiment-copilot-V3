# Netlify 部署步骤（详细版）

## 🚨 重要提示

如果 GitHub 推送失败，可以使用以下方法：

## 方法一：GitHub 推送（推荐）

### 1. 检查网络连接
```powershell
Test-NetConnection github.com -Port 443
```

### 2. 如果网络正常，尝试推送
```bash
git add .
git commit -m "更新 Netlify 配置"
git push origin main
```

### 3. 如果推送失败，尝试以下方法：

#### 方法 A：使用 SSH（如果已配置）
```bash
# 查看远程地址
git remote -v

# 如果使用 HTTPS，可以改为 SSH
git remote set-url origin git@github.com:MyraWang0406/phone-sentiment-copilot-V3.git
git push origin main
```

#### 方法 B：配置代理（如果使用代理）
```bash
# 设置代理（根据你的代理端口调整）
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890

# 推送
git push origin main

# 推送完成后，取消代理
git config --global --unset http.proxy
git config --global --unset https.proxy
```

#### 方法 C：使用 GitHub Desktop 或其他 Git 客户端
- 下载 GitHub Desktop：https://desktop.github.com/
- 使用图形界面推送代码

## 方法二：Netlify 手动部署（如果 GitHub 推送失败）

### 步骤 1：准备部署文件

1. **进入 netlify-deploy 文件夹**
   ```powershell
   cd netlify-deploy
   ```

2. **确认文件结构**
   应该包含以下文件：
   - `index.html` ✅
   - `netlify.toml` ✅
   - `README_部署说明.md`（可选）

### 步骤 2：压缩文件

**重要**：需要压缩 `netlify-deploy` 文件夹**内部的所有文件**，而不是压缩文件夹本身！

#### Windows PowerShell：
```powershell
# 在 netlify-deploy 文件夹内执行
Compress-Archive -Path * -DestinationPath ../netlify-deploy.zip
```

#### 或者手动操作：
1. 进入 `netlify-deploy` 文件夹
2. 全选所有文件（`index.html`、`netlify.toml` 等）
3. 右键 → "发送到" → "压缩(zipped)文件夹"
4. 将生成的 ZIP 文件重命名为 `netlify-deploy.zip`

### 步骤 3：在 Netlify 上传

1. **登录 Netlify**
   - 访问 https://app.netlify.com
   - 使用 GitHub 账号登录

2. **进入站点或创建新站点**
   - 如果已有站点：点击站点名称进入
   - 如果没有站点：点击 "Add new site" → "Deploy manually"

3. **上传 ZIP 文件**
   - 进入 "Deploys" 标签
   - 点击 "Trigger deploy" → "Deploy site"
   - 选择 "Deploy manually"
   - 将 `netlify-deploy.zip` 文件拖拽到上传区域
   - 等待部署完成（通常 1-2 分钟）

4. **验证部署**
   - 部署完成后，访问 Netlify 提供的域名
   - 检查页面是否正常加载
   - 打开浏览器开发者工具（F12），查看 Network 标签
   - 尝试刷新数据，检查 API 请求是否正常

### 步骤 4：检查部署配置

部署成功后，检查：

1. **站点设置** → **Build & deploy**
   - Publish directory: `.` 或留空
   - Build command: 留空或 `echo "No build needed"`

2. **站点设置** → **Domain management**
   - 可以自定义域名或使用 Netlify 提供的免费域名

## 方法三：通过 Netlify CLI（高级）

### 1. 安装 Netlify CLI
```bash
npm install -g netlify-cli
```

### 2. 登录
```bash
netlify login
```

### 3. 部署
```bash
cd netlify-deploy
netlify deploy --prod
```

## 🔍 故障排查

### 问题 1：页面加载但数据不显示

**可能原因**：
- API 代理配置错误
- Render 后端地址不正确
- CORS 问题

**解决方法**：
1. 检查 `netlify.toml` 中的 Render 后端地址是否正确
2. 打开浏览器开发者工具（F12）→ Network 标签
3. 查看 API 请求是否返回错误
4. 检查 Render 后端是否正常运行

### 问题 2：部署后显示 404

**可能原因**：
- 文件结构不正确
- `netlify.toml` 配置错误

**解决方法**：
1. 确认 ZIP 文件内包含 `index.html` 和 `netlify.toml`
2. 确认 `netlify.toml` 中的 `publish = "."` 配置正确

### 问题 3：GitHub 推送失败

**可能原因**：
- 网络连接问题
- 认证问题
- 权限问题

**解决方法**：
1. 检查网络连接
2. 使用 GitHub Desktop 或其他 Git 客户端
3. 使用手动部署方法

## ✅ 部署检查清单

- [ ] `netlify.toml` 中的 Render 后端地址已更新
- [ ] `index.html` 中的 API_BASE 配置正确（已自动切换）
- [ ] 所有文件已提交到 Git（如果使用 GitHub 部署）
- [ ] ZIP 文件包含所有必要文件（如果手动部署）
- [ ] Netlify 部署成功
- [ ] 页面可以正常访问
- [ ] API 请求正常工作

---

**如果遇到问题，请检查：**
1. Netlify 部署日志
2. 浏览器控制台错误信息
3. Network 标签中的 API 请求状态

